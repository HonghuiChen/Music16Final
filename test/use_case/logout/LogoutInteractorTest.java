package use_case.logout;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class LogoutInteractorTest {

    @Test
    void testExecute() {
        //no inputs, no outputs, only calls the presenter to switch the view, so guaranteed to work
        //for test in logic, see LogoutPresenterTest
        assert true;
    }
}